# GAP-03 evidence

Preserve and validate `address_prefix`: the per-deployment IPv4 mesh prefix
(FML-ADR-063) is carried from the mission package into generated configuration,
where it was previously dropped.

State: IMPLEMENTED. Owner: Claude. Awaiting independent red-team, then closure.

| Artifact | What it records |
| --- | --- |
| `implementation.md` | The change made, the failing-first demonstration, and how to reproduce it. |
