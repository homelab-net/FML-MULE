# Tools-tree sandbox APT configuration

`sources.list.d/` contains the deb822 source definition for authenticated,
dated Debian main, security, and builder-only backports archives selected by
`FML-ADR-081`.
